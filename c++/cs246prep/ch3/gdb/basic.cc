#include <iostream>

int main() {
    int j = 3;
    int k = 31;

    j += k;
    k = j * 2;
    std::cout << "hi" << std::endl;
// -g is the compiler flag for gdb
/*
run it with gdb ./PROGRAM_NAME
run will run the program


*/

/*
macOS does not support gdb, and uses lldb instead

run
next
step
continue
print
backtrace
quit

commands are the still the same but there are differences, i will just use the
linux ssh server instead

*/

}
